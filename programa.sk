Pessoa {

    nome: string;
    idade: int;
};
fun mostrarPessoa(p : Pessoa) : void {
    out(p.nome);
    out(p.idade);
}

fun mostrar(num: int) : void {
    outl(num);
}


var p: Pessoa = { "maria", 22};
var pessoas : vector<Pessoa>;
pessoas.append(p);

mostrarPessoa(p);
mostrar(1.0);
mostrar(2);